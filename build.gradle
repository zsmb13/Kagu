buildscript {
    ext.kotlin_version = '1.2.10'
    ext.koinjs_version = '0.1.3'
    ext.kagu_version = '0.2.1'
    ext.webDir = "${rootDir}/web"
    ext.devMode = project.hasProperty('devMode') ? devMode.toBoolean() : true
}

allprojects {
    repositories {
        mavenCentral()
        jcenter()

        if(!devMode.toBoolean()) {
            mavenLocal()
        }
    }
    buildscript {
        repositories {
            mavenCentral()
            jcenter()
        }
    }
}
